# Docker Lab Submission

## Docker Installation Verification

```bash
docker --version
Docker version 29.5.2

docker info

docker run hello-world

docker pull nginx

docker run -it -d --name=mynginx nginx

docker ps
```

## Output

Docker installed successfully on Ubuntu 24.04.

Nginx container created successfully.

Container status verified using docker ps.
